//
// CreditCardTypes.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
import Vapor

/**  */
public final class CreditCardTypes: Content {
    /** An array containing supported credit card types. */
    public var cardTypes: [String]?

    public init(cardTypes: [String]? = nil) {
        self.cardTypes = cardTypes
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case cardTypes
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encodeIfPresent(cardTypes, forKey: .cardTypes)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        cardTypes = try container.decodeIfPresent([String].self, forKey: .cardTypes)
    }
}

extension CreditCardTypes: Hashable {
    public static func == (lhs: CreditCardTypes, rhs: CreditCardTypes) -> Bool {
        lhs.cardTypes == rhs.cardTypes
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(cardTypes?.hashValue)
    }
}
