//
// BulkSendBatchStatus.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
import Vapor

/**  */
public final class BulkSendBatchStatus: Content {
    /** Identifier used to query the status of an individual bulk recipient batch. */
    public var batchId: String?
    /**  */
    public var batchName: String?
    /** The total number of items in the batch being queried. */
    public var batchSize: String?
    /**  */
    public var bulkErrors: [BulkSendErrorStatus]?
    /**  */
    public var envelopeIdOrTemplateId: String?
    /**  */
    public var envelopesUri: String?
    /** The number of entries with a status of `failed`.  */
    public var failed: String?
    /**  */
    public var mailingListId: String?
    /** The number of entries with a status of `queued`. */
    public var queued: String?
    /** The number of entries with a status of `sent`. */
    public var sent: String?
    /** The date on which the bulk envelope was created. */
    public var submittedDate: String?

    public init(batchId: String? = nil, batchName: String? = nil, batchSize: String? = nil, bulkErrors: [BulkSendErrorStatus]? = nil, envelopeIdOrTemplateId: String? = nil, envelopesUri: String? = nil, failed: String? = nil, mailingListId: String? = nil, queued: String? = nil, sent: String? = nil, submittedDate: String? = nil) {
        self.batchId = batchId
        self.batchName = batchName
        self.batchSize = batchSize
        self.bulkErrors = bulkErrors
        self.envelopeIdOrTemplateId = envelopeIdOrTemplateId
        self.envelopesUri = envelopesUri
        self.failed = failed
        self.mailingListId = mailingListId
        self.queued = queued
        self.sent = sent
        self.submittedDate = submittedDate
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case batchId
        case batchName
        case batchSize
        case bulkErrors
        case envelopeIdOrTemplateId
        case envelopesUri
        case failed
        case mailingListId
        case queued
        case sent
        case submittedDate
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encodeIfPresent(batchId, forKey: .batchId)
        try container.encodeIfPresent(batchName, forKey: .batchName)
        try container.encodeIfPresent(batchSize, forKey: .batchSize)
        try container.encodeIfPresent(bulkErrors, forKey: .bulkErrors)
        try container.encodeIfPresent(envelopeIdOrTemplateId, forKey: .envelopeIdOrTemplateId)
        try container.encodeIfPresent(envelopesUri, forKey: .envelopesUri)
        try container.encodeIfPresent(failed, forKey: .failed)
        try container.encodeIfPresent(mailingListId, forKey: .mailingListId)
        try container.encodeIfPresent(queued, forKey: .queued)
        try container.encodeIfPresent(sent, forKey: .sent)
        try container.encodeIfPresent(submittedDate, forKey: .submittedDate)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        batchId = try container.decodeIfPresent(String.self, forKey: .batchId)
        batchName = try container.decodeIfPresent(String.self, forKey: .batchName)
        batchSize = try container.decodeIfPresent(String.self, forKey: .batchSize)
        bulkErrors = try container.decodeIfPresent([BulkSendErrorStatus].self, forKey: .bulkErrors)
        envelopeIdOrTemplateId = try container.decodeIfPresent(String.self, forKey: .envelopeIdOrTemplateId)
        envelopesUri = try container.decodeIfPresent(String.self, forKey: .envelopesUri)
        failed = try container.decodeIfPresent(String.self, forKey: .failed)
        mailingListId = try container.decodeIfPresent(String.self, forKey: .mailingListId)
        queued = try container.decodeIfPresent(String.self, forKey: .queued)
        sent = try container.decodeIfPresent(String.self, forKey: .sent)
        submittedDate = try container.decodeIfPresent(String.self, forKey: .submittedDate)
    }
}

extension BulkSendBatchStatus: Hashable {
    public static func == (lhs: BulkSendBatchStatus, rhs: BulkSendBatchStatus) -> Bool {
        lhs.batchId == rhs.batchId &&
            lhs.batchName == rhs.batchName &&
            lhs.batchSize == rhs.batchSize &&
            lhs.bulkErrors == rhs.bulkErrors &&
            lhs.envelopeIdOrTemplateId == rhs.envelopeIdOrTemplateId &&
            lhs.envelopesUri == rhs.envelopesUri &&
            lhs.failed == rhs.failed &&
            lhs.mailingListId == rhs.mailingListId &&
            lhs.queued == rhs.queued &&
            lhs.sent == rhs.sent &&
            lhs.submittedDate == rhs.submittedDate
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(batchId?.hashValue)
        hasher.combine(batchName?.hashValue)
        hasher.combine(batchSize?.hashValue)
        hasher.combine(bulkErrors?.hashValue)
        hasher.combine(envelopeIdOrTemplateId?.hashValue)
        hasher.combine(envelopesUri?.hashValue)
        hasher.combine(failed?.hashValue)
        hasher.combine(mailingListId?.hashValue)
        hasher.combine(queued?.hashValue)
        hasher.combine(sent?.hashValue)
        hasher.combine(submittedDate?.hashValue)
    }
}
