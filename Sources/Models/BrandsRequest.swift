//
// BrandsRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
import Vapor

/** Details about one or more brands. */
public final class BrandsRequest: Content {
    /** A list of brands. */
    public var brands: [BrandRequest]?

    public init(brands: [BrandRequest]? = nil) {
        self.brands = brands
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case brands
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encodeIfPresent(brands, forKey: .brands)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        brands = try container.decodeIfPresent([BrandRequest].self, forKey: .brands)
    }
}

extension BrandsRequest: Hashable {
    public static func == (lhs: BrandsRequest, rhs: BrandsRequest) -> Bool {
        lhs.brands == rhs.brands
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(brands?.hashValue)
    }
}
