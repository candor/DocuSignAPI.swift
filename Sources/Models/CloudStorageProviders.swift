//
// CloudStorageProviders.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
import Vapor

/** The CloudStorageProviders resource provides methods that allow you to manage the cloud storage providers associate with an account. */
public final class CloudStorageProviders: Content {
    /** An Array containing the storage providers associated with the user. */
    public var storageProviders: [CloudStorageProvider]?

    public init(storageProviders: [CloudStorageProvider]? = nil) {
        self.storageProviders = storageProviders
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case storageProviders
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encodeIfPresent(storageProviders, forKey: .storageProviders)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        storageProviders = try container.decodeIfPresent([CloudStorageProvider].self, forKey: .storageProviders)
    }
}

extension CloudStorageProviders: Hashable {
    public static func == (lhs: CloudStorageProviders, rhs: CloudStorageProviders) -> Bool {
        lhs.storageProviders == rhs.storageProviders
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(storageProviders?.hashValue)
    }
}
