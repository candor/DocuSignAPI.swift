//
// ContactModRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
import Vapor

/** The request object containing the new information for the contacts. */
public final class ContactModRequest: Content {
    /** A list of contacts. */
    public var contactList: [Contact]?

    public init(contactList: [Contact]? = nil) {
        self.contactList = contactList
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case contactList
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encodeIfPresent(contactList, forKey: .contactList)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        contactList = try container.decodeIfPresent([Contact].self, forKey: .contactList)
    }
}

extension ContactModRequest: Hashable {
    public static func == (lhs: ContactModRequest, rhs: ContactModRequest) -> Bool {
        lhs.contactList == rhs.contactList
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(contactList?.hashValue)
    }
}
