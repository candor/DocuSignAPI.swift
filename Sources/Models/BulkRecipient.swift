//
// BulkRecipient.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
import Vapor

/**  */
public final class BulkRecipient: Content {
    /** If a value is provided, the recipient must enter the value as the access code to view and sign the envelope.   Maximum Length: 50 characters and it must conform to the account's access code format setting.  If blank, but the signer `accessCode` property is set in the envelope, then that value is used.  If blank and the signer `accessCode` property is not set, then the access code is not required. */
    public var accessCode: String?
    /** Specifies the recipient's email address.   Maximum length: 100 characters. */
    public var email: String?
    /** Array or errors. */
    public var errorDetails: [ErrorDetails]?
    /** Specifies the authentication check used for the signer. If blank then no authentication check is required for the signer. Only one value can be used in this field.  The acceptable values are:  * KBA: Enables the normal ID check authentication set up for your account. * Phone: Enables phone authentication. * SMS: Enables SMS authentication. */
    public var identification: String?
    /** Specifies the recipient's name.   Maximum length: 50 characters. */
    public var name: String?
    /** A note sent to the recipient in the signing email. This note is unique to this recipient. In the user interface, it appears near the upper left corner of the document on the signing screen.  Maximum Length: 1000 characters.  */
    public var note: String?
    /** This is only used if the Identification field value is Phone or SMS. The value for this field can be a valid telephone number or, if Phone, usersupplied (SMS authentication cannot use a user supplied number). Parenthesis and dashes can be used in the telephone number.  If `usersupplied` is used, the signer supplies his or her own telephone number. */
    public var phoneNumber: String?
    /**  */
    public var recipientSignatureProviderInfo: [BulkRecipientSignatureProvider]?
    /**  */
    public var rowNumber: String?
    /** Specifies values used to populate recipient tabs with information. This allows each bulk recipient signer to have different values for their associated tabs. Any number of `tabLabel` columns can be added to the bulk recipient file.  The information used in the bulk recipient file header must be the same as the `tabLabel` for the tab.  The values entered in this column are automatically inserted into the corresponding tab for the recipient in the same row.  Note that this option cannot be used for tabs that do not have data or that are automatically populated data such as Signature, Full Name, Email Address, Company, Title, and Date Signed tabs. */
    public var tabLabels: [BulkRecipientTabLabel]?

    public init(accessCode: String? = nil, email: String? = nil, errorDetails: [ErrorDetails]? = nil, identification: String? = nil, name: String? = nil, note: String? = nil, phoneNumber: String? = nil, recipientSignatureProviderInfo: [BulkRecipientSignatureProvider]? = nil, rowNumber: String? = nil, tabLabels: [BulkRecipientTabLabel]? = nil) {
        self.accessCode = accessCode
        self.email = email
        self.errorDetails = errorDetails
        self.identification = identification
        self.name = name
        self.note = note
        self.phoneNumber = phoneNumber
        self.recipientSignatureProviderInfo = recipientSignatureProviderInfo
        self.rowNumber = rowNumber
        self.tabLabels = tabLabels
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case accessCode
        case email
        case errorDetails
        case identification
        case name
        case note
        case phoneNumber
        case recipientSignatureProviderInfo
        case rowNumber
        case tabLabels
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encodeIfPresent(accessCode, forKey: .accessCode)
        try container.encodeIfPresent(email, forKey: .email)
        try container.encodeIfPresent(errorDetails, forKey: .errorDetails)
        try container.encodeIfPresent(identification, forKey: .identification)
        try container.encodeIfPresent(name, forKey: .name)
        try container.encodeIfPresent(note, forKey: .note)
        try container.encodeIfPresent(phoneNumber, forKey: .phoneNumber)
        try container.encodeIfPresent(recipientSignatureProviderInfo, forKey: .recipientSignatureProviderInfo)
        try container.encodeIfPresent(rowNumber, forKey: .rowNumber)
        try container.encodeIfPresent(tabLabels, forKey: .tabLabels)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        accessCode = try container.decodeIfPresent(String.self, forKey: .accessCode)
        email = try container.decodeIfPresent(String.self, forKey: .email)
        errorDetails = try container.decodeIfPresent([ErrorDetails].self, forKey: .errorDetails)
        identification = try container.decodeIfPresent(String.self, forKey: .identification)
        name = try container.decodeIfPresent(String.self, forKey: .name)
        note = try container.decodeIfPresent(String.self, forKey: .note)
        phoneNumber = try container.decodeIfPresent(String.self, forKey: .phoneNumber)
        recipientSignatureProviderInfo = try container.decodeIfPresent([BulkRecipientSignatureProvider].self, forKey: .recipientSignatureProviderInfo)
        rowNumber = try container.decodeIfPresent(String.self, forKey: .rowNumber)
        tabLabels = try container.decodeIfPresent([BulkRecipientTabLabel].self, forKey: .tabLabels)
    }
}

extension BulkRecipient: Hashable {
    public static func == (lhs: BulkRecipient, rhs: BulkRecipient) -> Bool {
        lhs.accessCode == rhs.accessCode &&
            lhs.email == rhs.email &&
            lhs.errorDetails == rhs.errorDetails &&
            lhs.identification == rhs.identification &&
            lhs.name == rhs.name &&
            lhs.note == rhs.note &&
            lhs.phoneNumber == rhs.phoneNumber &&
            lhs.recipientSignatureProviderInfo == rhs.recipientSignatureProviderInfo &&
            lhs.rowNumber == rhs.rowNumber &&
            lhs.tabLabels == rhs.tabLabels
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(accessCode?.hashValue)
        hasher.combine(email?.hashValue)
        hasher.combine(errorDetails?.hashValue)
        hasher.combine(identification?.hashValue)
        hasher.combine(name?.hashValue)
        hasher.combine(note?.hashValue)
        hasher.combine(phoneNumber?.hashValue)
        hasher.combine(recipientSignatureProviderInfo?.hashValue)
        hasher.combine(rowNumber?.hashValue)
        hasher.combine(tabLabels?.hashValue)
    }
}
