//
// UserSocialIdResult.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
import Vapor

/**  */
public final class UserSocialIdResult: Content {
    /** Contains properties that map a DocuSign user to a social account such as Facebook or Yahoo. */
    public var socialAccountInformation: [SocialAccountInformation]?
    /** The ID of the user to access. Generally this is the ID of the current authenticated user, but if the authenticated user is an Administrator on the account, `userId` can represent another user whom the Administrator is accessing.  */
    public var userId: String?

    public init(socialAccountInformation: [SocialAccountInformation]? = nil, userId: String? = nil) {
        self.socialAccountInformation = socialAccountInformation
        self.userId = userId
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case socialAccountInformation
        case userId
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encodeIfPresent(socialAccountInformation, forKey: .socialAccountInformation)
        try container.encodeIfPresent(userId, forKey: .userId)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        socialAccountInformation = try container.decodeIfPresent([SocialAccountInformation].self, forKey: .socialAccountInformation)
        userId = try container.decodeIfPresent(String.self, forKey: .userId)
    }
}

extension UserSocialIdResult: Hashable {
    public static func == (lhs: UserSocialIdResult, rhs: UserSocialIdResult) -> Bool {
        lhs.socialAccountInformation == rhs.socialAccountInformation &&
            lhs.userId == rhs.userId
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(socialAccountInformation?.hashValue)
        hasher.combine(userId?.hashValue)
    }
}
