//
// BulkSendingListSummaries.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
import Vapor

/** This complex type contains summaries that provide basic information about the bulk send lists that belong to the current user. */
public final class BulkSendingListSummaries: Content {
    /** An array of `bulkSendingListSummary` objects where each summary provides basic information about a bulk send list that belongs to the current user. */
    public var bulkListSummaries: [BulkSendingListSummary]?

    public init(bulkListSummaries: [BulkSendingListSummary]? = nil) {
        self.bulkListSummaries = bulkListSummaries
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case bulkListSummaries
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encodeIfPresent(bulkListSummaries, forKey: .bulkListSummaries)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        bulkListSummaries = try container.decodeIfPresent([BulkSendingListSummary].self, forKey: .bulkListSummaries)
    }
}

extension BulkSendingListSummaries: Hashable {
    public static func == (lhs: BulkSendingListSummaries, rhs: BulkSendingListSummaries) -> Bool {
        lhs.bulkListSummaries == rhs.bulkListSummaries
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(bulkListSummaries?.hashValue)
    }
}
