//
// SignatureGroup.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
import Vapor

/**  */
public final class SignatureGroup: Content {
    /** The ID of the group being accessed. */
    public var groupId: String?
    /** The name of the group. The search_text provided in the call automatically performs a wild card search on group_name. */
    public var groupName: String?
    /** Indicates whether the property is editable. Valid values are:  - `editable` - `read_only` */
    public var rights: String?

    public init(groupId: String? = nil, groupName: String? = nil, rights: String? = nil) {
        self.groupId = groupId
        self.groupName = groupName
        self.rights = rights
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case groupId
        case groupName
        case rights
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encodeIfPresent(groupId, forKey: .groupId)
        try container.encodeIfPresent(groupName, forKey: .groupName)
        try container.encodeIfPresent(rights, forKey: .rights)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        groupId = try container.decodeIfPresent(String.self, forKey: .groupId)
        groupName = try container.decodeIfPresent(String.self, forKey: .groupName)
        rights = try container.decodeIfPresent(String.self, forKey: .rights)
    }
}

extension SignatureGroup: Hashable {
    public static func == (lhs: SignatureGroup, rhs: SignatureGroup) -> Bool {
        lhs.groupId == rhs.groupId &&
            lhs.groupName == rhs.groupName &&
            lhs.rights == rhs.rights
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(groupId?.hashValue)
        hasher.combine(groupName?.hashValue)
        hasher.combine(rights?.hashValue)
    }
}
