//
// PowerFormFormDataEnvelope.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
import Vapor

/**  */
public final class PowerFormFormDataEnvelope: Content {
    /** The envelope's GUID.   Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`  */
    public var envelopeId: String?
    /** An array of recipient objects that provides details about the recipients of the envelope. */
    public var recipients: [PowerFormFormDataRecipient]?

    public init(envelopeId: String? = nil, recipients: [PowerFormFormDataRecipient]? = nil) {
        self.envelopeId = envelopeId
        self.recipients = recipients
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case envelopeId
        case recipients
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encodeIfPresent(envelopeId, forKey: .envelopeId)
        try container.encodeIfPresent(recipients, forKey: .recipients)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        envelopeId = try container.decodeIfPresent(String.self, forKey: .envelopeId)
        recipients = try container.decodeIfPresent([PowerFormFormDataRecipient].self, forKey: .recipients)
    }
}

extension PowerFormFormDataEnvelope: Hashable {
    public static func == (lhs: PowerFormFormDataEnvelope, rhs: PowerFormFormDataEnvelope) -> Bool {
        lhs.envelopeId == rhs.envelopeId &&
            lhs.recipients == rhs.recipients
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(envelopeId?.hashValue)
        hasher.combine(recipients?.hashValue)
    }
}
