//
// SocialAuthentication.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
import Vapor

/**  */
public final class SocialAuthentication: Content {
    /** Reserved for DocuSign. */
    public var authentication: String?

    public init(authentication: String? = nil) {
        self.authentication = authentication
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case authentication
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encodeIfPresent(authentication, forKey: .authentication)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        authentication = try container.decodeIfPresent(String.self, forKey: .authentication)
    }
}

extension SocialAuthentication: Hashable {
    public static func == (lhs: SocialAuthentication, rhs: SocialAuthentication) -> Bool {
        lhs.authentication == rhs.authentication
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(authentication?.hashValue)
    }
}
