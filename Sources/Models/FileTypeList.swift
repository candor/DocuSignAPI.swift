//
// FileTypeList.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
import Vapor

/**  */
public final class FileTypeList: Content {
    /** A collection of file types. */
    public var fileTypes: [FileType]?

    public init(fileTypes: [FileType]? = nil) {
        self.fileTypes = fileTypes
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case fileTypes
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encodeIfPresent(fileTypes, forKey: .fileTypes)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        fileTypes = try container.decodeIfPresent([FileType].self, forKey: .fileTypes)
    }
}

extension FileTypeList: Hashable {
    public static func == (lhs: FileTypeList, rhs: FileTypeList) -> Bool {
        lhs.fileTypes == rhs.fileTypes
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(fileTypes?.hashValue)
    }
}
