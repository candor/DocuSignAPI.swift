//
// TabMetadataList.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
import Vapor

/**  */
public final class TabMetadataList: Content {
    /** A list of tabs, which are represented graphically as symbols on documents at the time of signing. Tabs show recipients where to sign, initial, or enter data. They may also display data to the recipients. */
    public var tabs: [TabMetadata]?

    public init(tabs: [TabMetadata]? = nil) {
        self.tabs = tabs
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case tabs
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encodeIfPresent(tabs, forKey: .tabs)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        tabs = try container.decodeIfPresent([TabMetadata].self, forKey: .tabs)
    }
}

extension TabMetadataList: Hashable {
    public static func == (lhs: TabMetadataList, rhs: TabMetadataList) -> Bool {
        lhs.tabs == rhs.tabs
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(tabs?.hashValue)
    }
}
