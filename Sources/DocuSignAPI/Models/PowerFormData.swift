//
// PowerFormData.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import AnyCodable
import Foundation
import Vapor

/** Data that recipients have entered in PowerForm fields. */
public final class PowerFormData: Content, Hashable {
    /** The envelope ID of the envelope status that failed to post. */
    public var envelopeId: String?
    /** An array of powerform recipients. */
    public var recipients: [PowerFormFormDataRecipient]?

    public init(envelopeId: String? = nil, recipients: [PowerFormFormDataRecipient]? = nil) {
        self.envelopeId = envelopeId
        self.recipients = recipients
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case envelopeId
        case recipients
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(envelopeId, forKey: .envelopeId)
        try container.encodeIfPresent(recipients, forKey: .recipients)
    }

    public static func == (lhs: PowerFormData, rhs: PowerFormData) -> Bool {
        lhs.envelopeId == rhs.envelopeId &&
            lhs.recipients == rhs.recipients
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(envelopeId?.hashValue)
        hasher.combine(recipients?.hashValue)
    }
}
