//
// AccountSharedAccess.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import AnyCodable
import Foundation
import Vapor

/** Contains shared access information. */
public final class AccountSharedAccess: Content, Hashable {
    /** The account ID associated with the envelope. */
    public var accountId: String?
    /** The last index position in the result set.  */
    public var endPosition: String?
    public var errorDetails: ErrorDetails?
    /** The URI for the next chunk of records based on the search request. It is `null` if this is the last set of results for the search.  */
    public var nextUri: String?
    /** The URI for the prior chunk of records based on the search request. It is `null` if this is the first set of results for the search.  */
    public var previousUri: String?
    /** The number of results in this response. Because you can filter which entries are included in the response, this value is always less than or equal to the `totalSetSize`. */
    public var resultSetSize: String?
    /** A list of shared access information of envelope and templates for the users specified in the request. */
    public var sharedAccess: [MemberSharedItems]?
    /** The starting index position of the current result set. */
    public var startPosition: String?
    /** The total number of items in the result set. This value is always greater than or equal to the value of `resultSetSize`. */
    public var totalSetSize: String?

    public init(accountId: String? = nil, endPosition: String? = nil, errorDetails: ErrorDetails? = nil, nextUri: String? = nil, previousUri: String? = nil, resultSetSize: String? = nil, sharedAccess: [MemberSharedItems]? = nil, startPosition: String? = nil, totalSetSize: String? = nil) {
        self.accountId = accountId
        self.endPosition = endPosition
        self.errorDetails = errorDetails
        self.nextUri = nextUri
        self.previousUri = previousUri
        self.resultSetSize = resultSetSize
        self.sharedAccess = sharedAccess
        self.startPosition = startPosition
        self.totalSetSize = totalSetSize
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case accountId
        case endPosition
        case errorDetails
        case nextUri
        case previousUri
        case resultSetSize
        case sharedAccess
        case startPosition
        case totalSetSize
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(accountId, forKey: .accountId)
        try container.encodeIfPresent(endPosition, forKey: .endPosition)
        try container.encodeIfPresent(errorDetails, forKey: .errorDetails)
        try container.encodeIfPresent(nextUri, forKey: .nextUri)
        try container.encodeIfPresent(previousUri, forKey: .previousUri)
        try container.encodeIfPresent(resultSetSize, forKey: .resultSetSize)
        try container.encodeIfPresent(sharedAccess, forKey: .sharedAccess)
        try container.encodeIfPresent(startPosition, forKey: .startPosition)
        try container.encodeIfPresent(totalSetSize, forKey: .totalSetSize)
    }

    public static func == (lhs: AccountSharedAccess, rhs: AccountSharedAccess) -> Bool {
        lhs.accountId == rhs.accountId &&
            lhs.endPosition == rhs.endPosition &&
            lhs.errorDetails == rhs.errorDetails &&
            lhs.nextUri == rhs.nextUri &&
            lhs.previousUri == rhs.previousUri &&
            lhs.resultSetSize == rhs.resultSetSize &&
            lhs.sharedAccess == rhs.sharedAccess &&
            lhs.startPosition == rhs.startPosition &&
            lhs.totalSetSize == rhs.totalSetSize
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(accountId?.hashValue)
        hasher.combine(endPosition?.hashValue)
        hasher.combine(errorDetails?.hashValue)
        hasher.combine(nextUri?.hashValue)
        hasher.combine(previousUri?.hashValue)
        hasher.combine(resultSetSize?.hashValue)
        hasher.combine(sharedAccess?.hashValue)
        hasher.combine(startPosition?.hashValue)
        hasher.combine(totalSetSize?.hashValue)
    }
}
